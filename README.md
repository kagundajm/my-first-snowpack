## Running Snowpack Build and Watch Does Not Generate CSS File

Running `npm run build` generates _\_snowpack/pkg/bootstrap/dist/css/bootstrap.css_ file and the correct folder structure.

![alt="snowpack build generates css files"](/snowpack-dist-01.png)

However, running `npm run watch` fails to generate the _\_snowpack/pkg/bootstrap/dist/css/bootstrap.css_  and the  folders.accordion

![alt="snowpack build generates css files"](/snowpack-dist-02.png)